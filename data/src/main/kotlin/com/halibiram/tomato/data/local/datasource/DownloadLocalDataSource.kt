package com.halibiram.tomato.data.local.datasource

// import com.halibiram.tomato.core.database.dao.DownloadDao // From core:database
// import com.halibiram.tomato.core.database.entity.DownloadEntity // From core:database
// import kotlinx.coroutines.flow.Flow
// import javax.inject.Inject

// class DownloadLocalDataSource @Inject constructor(private val downloadDao: DownloadDao) {
//    fun getDownloadById(mediaId: String): Flow<DownloadEntity?> = downloadDao.getDownloadById(mediaId)
//    fun getAllDownloads(): Flow<List<DownloadEntity>> = downloadDao.getAllDownloads()
//    suspend fun insertDownload(download: DownloadEntity) = downloadDao.insertDownload(download)
//    suspend fun updateDownload(download: DownloadEntity) = downloadDao.updateDownload(download)
//    suspend fun deleteDownload(mediaId: String) = downloadDao.deleteDownload(mediaId)
// }

// Placeholder
interface DownloadLocalDataSource {
    // fun getDownloadById(mediaId: String): Any
    // fun getAllDownloads(): Any
    // suspend fun insertDownload(download: Any)
    // suspend fun updateDownload(download: Any)
    // suspend fun deleteDownload(mediaId: String)
}
