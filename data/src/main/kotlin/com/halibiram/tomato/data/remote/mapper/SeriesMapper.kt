package com.halibiram.tomato.data.remote.mapper

// import com.halibiram.tomato.data.remote.dto.SeriesDto
// import com.halibiram.tomato.domain.model.Series // From domain module
// import javax.inject.Inject

// class SeriesMapper @Inject constructor() {
//    fun toDomain(dto: SeriesDto): Series {
//        return Series(
//            id = dto.id.toString(),
//            title = dto.name, // Note: DTOs often use 'name' for series
//            posterPath = dto.posterPath,
//            // ... map other fields
//        )
//    }
//
//    fun toDomainList(dtos: List<SeriesDto>): List<Series> {
//        return dtos.map { toDomain(it) }
//    }
// }

// Placeholder
interface SeriesMapper {
    // fun toDomain(dto: Any): Any // Placeholder for SeriesDto -> Series
    // fun toDomainList(dtos: List<Any>): List<Any>
}
