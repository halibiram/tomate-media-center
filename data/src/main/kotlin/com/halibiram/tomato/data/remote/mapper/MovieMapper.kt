package com.halibiram.tomato.data.remote.mapper

// import com.halibiram.tomato.data.remote.dto.MovieDto
// import com.halibiram.tomato.domain.model.Movie // From domain module
// import javax.inject.Inject

// class MovieMapper @Inject constructor() {
//    fun toDomain(dto: MovieDto): Movie {
//        return Movie(
//            id = dto.id.toString(),
//            title = dto.title,
//            posterPath = dto.posterPath,
//            // ... map other fields
//        )
//    }
//
//    fun toDomainList(dtos: List<MovieDto>): List<Movie> {
//        return dtos.map { toDomain(it) }
//    }
// }

// Placeholder
interface MovieMapper {
    // fun toDomain(dto: Any): Any // Placeholder for MovieDto -> Movie
    // fun toDomainList(dtos: List<Any>): List<Any>
}
