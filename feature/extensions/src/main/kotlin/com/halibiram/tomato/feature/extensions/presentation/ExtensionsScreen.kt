package com.halibiram.tomato.feature.extensions.presentation

import androidx.compose.runtime.Composable

// ExtensionsScreen Composable
@Composable
fun ExtensionsScreen(
    // viewModel: ExtensionsViewModel = hiltViewModel() // Example
) {
    // Screen content to list, manage, and browse extensions
}
