package com.halibiram.tomato.feature.player.presentation.component

import androidx.compose.runtime.Composable

// PlayerOverlay Composable
@Composable
fun PlayerOverlay(
    // visible: Boolean,
    // title: String?,
    // onBack: () -> Unit
) {
    // if (visible) {
    //     Box { /* Overlay content: Title, back button, etc. */ }
    // }
}
